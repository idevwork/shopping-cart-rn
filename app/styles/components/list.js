import { color } from '../abstracts/variables'

export const list = {
  row: {
    alignItems: 'center',
    borderBottomWidth: 1,
    borderColor: color.dark400,
    flexDirection: 'row',
    justifyContent: 'space-between',
    padding: 10,
    paddingVertical: 15
  },
  rowTitle: {
    marginRight: 50
  },
  rowContent: {
    alignItems: 'center',
    flexDirection: 'row'
  }
}
