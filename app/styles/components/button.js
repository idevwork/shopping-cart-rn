import { color } from '../abstracts/variables'

export const button = {
  action: {
    alignItems: 'center',
    backgroundColor: color.dark500,
    borderRadius: 1000,
    height: 30,
    justifyContent: 'center',
    marginHorizontal: 10,
    width: 30
  },
  actionText: {
    color: color.white
  },
  normal: {},
  normalText: {}
}
