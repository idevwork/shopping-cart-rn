import { color } from '../common/variables'

export const listStyles = {
  row: {
    alignItems: 'center',
    borderBottomWidth: 1,
    borderColor: color.dark400,
    flexDirection: 'row',
    justifyContent: 'space-between',
    padding: 10,
    paddingVertical: 15
  },
  rowTitle: {
    paddingRight: 50
  },
  rowContent: {
    alignItems: 'center',
    flexDirection: 'row'
  }
}
